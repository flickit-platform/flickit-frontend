import React from "react";
import Typography from "@mui/material/Typography";
import Box from "@mui/material/Box";
import { Trans } from "react-i18next";

const AssessmentKitAbout = () => {
  return (
    <Box>
      <Typography>
        <Trans i18nKey={""} />
      </Typography>
    </Box>
  );
};

export default AssessmentKitAbout;